<li>
    {{name}}

    <button class="not-devoured" data-id="{{id}}" data-newsleep="{{devoured}}">
        {{#if devoured}}TIME TO EAT!{{else}}YUM!{{/if}}
    </button>

    <button class="devoured" data-id="{{this.id}}">Devour!!</button>
</li>